module.exports = function(app) {
  /*app.dataSources.SecretaryDS.automigrate('SecretaryUser', function(err) {
    if (err) throw err;

    app.models.SecretaryUser.create([{
      firstname : "firstname",
      lastname : "lastname",
      username : "username",
      email:"email@fr",
      password:"password"
    }, ], function(err, secretaryUsers) {
      if (err) throw err;

      console.log('Models created: \n', secretaryUsers);
    });

  });*/
};